---
type: concept
area:
  - Cryptography
aliases:
  - "Криптосистема RSA"
status: learning
---
# RSA

## Суть

RSA строится в кольце вычетов по модулю `n=pq`. Открытая экспонента e и секретная d выбираются взаимно обратными по модулю группового показателя; открытая операция возводит в степень e, закрытая — в степень d.

## Как устроено

- Генерация: выбрать независимые простые p и q, вычислить n и функцию от `(p−1)(q−1)`, выбрать e и найти d.
- Учебное шифрование: `c=m^e mod n`, расшифрование: `m=c^d mod n`.
- Корректность для взаимно простых сообщений следует из теоремы Эйлера; общий случай разбирается по p и q и объединяется CRT.
- Закрытую операцию ускоряют отдельными вычислениями по p и q.

## Ключевая формула

Для $n=pq$ выбирают $e$ и $d$ так, что $ed\equiv1\pmod{\varphi(n)}$. Тогда $$c=m^e\pmod n,\qquad m=c^d\pmod n.$$

<!-- reviewed-notation:start -->
**Обозначения и условия.** p,q — разные простые; n=pq, phi(n)=(p−1)(q−1); e — положительная открытая экспонента, взаимно простая с phi(n); d — её обратный mod phi(n). m,c — целые представители 0…n−1. Это сырые модульные операции: безопасное кодирование, сериализация байтов и ограничения прикладной схемы задаются отдельно.
<!-- reviewed-notation:end -->

## Практический разбор

<!-- reviewed-example:start -->
*Происхождение:* Составленный учебный пример; не экспериментальный результат курса.

**Исходные данные.** Учебные p=11,q=13,n=143; phi=120; e=7,d=103; m=9.

1. Проверить 7·103=721≡1 mod 120.
2. 9²≡81,9⁴≡126,9⁷≡126·81·9≡48 mod 143.
3. Для расшифрования: 48²≡16,48⁴≡113,48⁸≡42,48¹⁶≡48,48³²≡16,48⁶⁴≡113. Поскольку 103=64+32+4+2+1, произведение 113·16·113·16·48 mod 143=9.

**Результат.** m=9 зашифровалось в 48 и восстановилось.

**Проверка.** Прямой модульный расчёт pow(48,103,143) даёт 9.

**Граница применимости.** Это учебное ядро без безопасного кодирования. m должен быть 0…142; одинаковый m при сыром RSA даёт одинаковый c.
<!-- reviewed-example:end -->

### Дополнительное пояснение из конспекта

На визуально проверенной странице 13 лекции 17–18 доказательство делит сообщения на случаи `gcd(m,n)=1` и кратности одному из простых факторов, после чего применяет CRT. Это объясняет корректность для всех классов, а не только для единиц кольца.

## Ограничения и безопасность

- Textbook RSA детерминирован и алгебраически податлив; нужны стандартизованные randomized padding/encoding schemes.
- Повторные простые, слабая энтропия, малые параметры и утечки времени закрытой операции компрометируют ключ.
- Ошибка CRT-ветви может раскрыть фактор n.

## Связи

- [[Euler Totient and Fermat-Euler Theorems]]
- [[Chinese Remainder Theorem]]
- [[Primality Testing and Miller-Rabin]]
- [[Side-Channel Attacks]]

## Самопроверка

1. Какую задачу решает Криптосистема RSA и какие входные предпосылки для этого нужны?
2. Воспроизведите основной механизм, вычислительные шаги или поток данных без подсказки.
3. Назовите главное ограничение или ошибку применения и способ её обнаружить либо предотвратить.

## Источники курса

- [[Source - 2024_Лекция 17-18]], стр. 9–17.
- [[Source - Тема №1 Введение(1)]], абз. 552–611.
